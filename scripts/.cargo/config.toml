# Output for the tools under scripts/, kept out of daegun's own target/ at the repo root. Cargo finds
# this by walking up from the working directory, and the path is relative to scripts/, not to it.
[build]
target-dir = ".out/target"
